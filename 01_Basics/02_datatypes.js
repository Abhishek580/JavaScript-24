"use strict" // treat all JS code as newer version
 // but now a days we don;t have to write this as sab automatically strict mode me chalta hai..

 let name = "Abhishek"
 let age = "26"
 let isLoggedIs = false


 /*
Primitive datatype

 1. number (Range = 2 to the power 53)
 2. bigint
 3. string
 4. boolean (true/false)
 5. null [
       * this is a standalone value
       * null is type
       * it is a representation of empty value
       * ]
 6. undefiend ()
 7. symbol (unique)
 */

console.log(typeof(null)) // is an object;
console.log(typeof(undefined)) // is an undefined;
 